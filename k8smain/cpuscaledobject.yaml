apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  labels:
    argocd.argoproj.io/instance: sdomain
  name: cpu-scaledobject
  namespace: sdomain
spec:
  advanced:
    horizontalPodAutoscalerConfig:
      behavior: cpu
      cpu:
        utilization: 50
  cooldownPeriod: 30
  maxReplicaCount: 10
  minReplicaCount: 1
  pollingInterval: 5
  scaleTargetRef:
    deploymentName: sdomain