apiVersion: v1
kind: Service
metadata:
  name: sdomainRolloutService
  namespace: sdomain
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: sdomain